datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       role String @default("USER")
  name     String
  email    String @unique
  password String
}
